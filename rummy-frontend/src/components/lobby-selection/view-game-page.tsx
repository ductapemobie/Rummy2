import axios from "axios";

export default function ViewGamePage(props: { gameId: number; }){
    const gameId = props.gameId;

    async function getGameData(){
        //const result = axios.get()
    }

    return <></>
}